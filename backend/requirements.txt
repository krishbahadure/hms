# Backend Python Dependencies
# Install with: pip install -r requirements.txt

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database (async MySQL)
sqlalchemy==2.0.30
aiomysql==0.2.0           # Async MySQL driver for SQLAlchemy
pymysql==1.1.0            # Sync driver (used by Alembic for migrations)
alembic==1.13.1           # DB migrations

# PDF Generation (saved to local filesystem)
reportlab==4.1.0

# Environment variable management
python-dotenv==1.0.1

# Pydantic v2 (bundled with FastAPI, but pin for stability)
pydantic==2.7.1

# Security (JWT auth – wire up as needed)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0             # Test client for FastAPI
